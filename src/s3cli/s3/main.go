package s3

func main() {
	// configPath, nonFlagArgs, err := fetchConfigurationPath(os.Args[1:])
	// if err != nil {
	// 	log.Panicln(err)
	// }
	// command, src, dst, err := validateArguments(nonFlagArgs)
	// if err != nil {
	// 	log.Panicln(err)
	// }
	// Read configuration
	// Create client with config
}
